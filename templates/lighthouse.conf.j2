server {
    listen 80;
    server_name localhost;

    access_log   var/log/nginx/{{ access_log_name }}.log  main;

    location / {
        root {{ lighthouse_location_dir }}
        index index.html;
    }
}